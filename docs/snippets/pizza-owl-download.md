```bash
# Pizza is not bundled in pip/crates.io installs — fetch once:
curl -L -o pizza.owl \
  https://github.com/owlcs/pizza-ontology/raw/refs/heads/master/pizza.owl
```

From a repository clone you can instead run `./benchmarks/scripts/download.sh`.
