!!! warning "Before you integrate"
    OntoLogos maps a **subset** of OWL axioms into its core model — `axiom_count()` will not match Protégé totals. Remote `owl:imports` are never fetched. Read [Known limitations](../guides/known-limitations.md) and [Supported constructs](../reference/supported-constructs.md) before production cutover.
