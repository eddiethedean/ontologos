# v1.0.0 — HermiT parity milestone

> **Status:** Ready to tag **v1.0.0** — engineering gates green; publish via Release workflow on tag push.

## Highlights

- **OWL 2 DL:** `ontologos-dl` stable — JVM-free HermiT replacement for production embedders
- **HermiT conformance:** **100%** catalog parity (**1048** active tests @ 30s); **0** generated catalog `#[ignore]`
- **SWRL:** DLSafe rules via `ontologos-swrl` (stable on published 1.0.0)
- **Facade:** unified `classify`, `check_consistency`, entailment — `#[non_exhaustive]` `ClassifyOutcome`
- **Python:** `profile="dl"` / `profile="swrl"`; `ontologos.types` TypedDict exports; Pyright CI
- **JSON snapshot v3** (v2 read supported); removed shim crates (`ontologos-rdfs`, `ontologos-abox`, `ontologos-query`, `ontologos-watch`)

## Pre-release checklist

- [x] Workspace and PyPI version **1.0.0** ([Cargo.toml](../../Cargo.toml), [pyproject.toml](../../crates/ontologos-py/pyproject.toml))
- [x] [CHANGELOG.md](../../CHANGELOG.md) dated **1.0.0** section; empty `[Unreleased]`
- [x] `check-1.0-release-gates.sh` green
- [x] `check-hermit-parity-phases.sh` green
- [x] fmt, clippy (`-D warnings`), workspace tests
- [ ] Annotated tag `v1.0.0` pushed → Release workflow (crates.io + PyPI)
- [ ] Post-tag: [Post-1.0 doc update](../../docs/project/post-1.0-doc-update.md)

## Published artifacts (on tag)

- **crates.io (12 crates):** `ontologos-core`, `ontologos-profile`, `ontologos-bridge`, `ontologos-parser`, `ontologos-rl`, `ontologos-alc`, `ontologos-el`, `ontologos-dl`, `ontologos-swrl`, `ontologos-explain`, `ontologos-ql`, `ontologos-facade` at **1.0.0**
- **PyPI:** `ontologos` **1.0.0** (Linux/macOS/Windows wheels + sdist)
- **CLI:** `cargo install --git https://github.com/eddiethedean/ontologos --tag v1.0.0 ontologos-cli`

## Upgrade from v0.9.0

Bump all `ontologos-*` crates together:

```toml
ontologos-core = "1.0.0"
ontologos-parser = "1.0.0"
ontologos-facade = "1.0.0"
# … all ontologos-* pins at 1.0.0
```

Python: `pip install ontologos==1.0.0`

**Breaking changes:**

- `Reasoner::classify()` removed from core — use `ontologos_facade::classify`
- `ontologos-rdfs` → `ontologos-rl` (`rdfs` module)
- `ontologos-query` → `ontologos-ql`
- JSON writers emit format **v3**

See [v0.9.x → v1.0.0 migration](../../docs/migration/v0.9.x-to-v1.0.0.md) and [CHANGELOG.md](../../CHANGELOG.md).

## Verify after publish

```bash
cargo new ontologos-dl-smoke && cd ontologos-dl-smoke
cargo add ontologos-dl@1.0.0 ontologos-parser@1.0.0 ontologos-facade@1.0.0
pip install ontologos==1.0.0
ontologos classify --profile dl family.owl
```
