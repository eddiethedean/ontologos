# Integration Tests

Workspace-level integration tests will be added in v0.2 when the parser can load benchmark ontologies from `benchmarks/manifest.toml`.

Core model integration tests live in `crates/ontologos-core/tests/`.
